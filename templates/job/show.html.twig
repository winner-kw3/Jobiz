{% extends 'base.html.twig' %}

{% block title %}Job{% endblock %}

{% block body %}
    <h1>Job</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ job.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ job.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ job.description }}</td>
            </tr>
            <tr>
                <th>Country</th>
                <td>{{ job.country }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ job.city }}</td>
            </tr>
            <tr>
                <th>RemoteAllowed</th>
                <td>{{ job.remoteAllowed ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>SalaryRange</th>
                <td>{{ job.salaryRange }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_job_index') }}">back to list</a>

    <a href="{{ path('app_job_edit', {'id': job.id}) }}">edit</a>

    {{ include('job/_delete_form.html.twig') }}
{% endblock %}
