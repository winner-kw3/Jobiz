{% extends 'base.html.twig' %}

{% block title %}JobType{% endblock %}

{% block body %}
    <h1>JobType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ job_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ job_type.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_job_type_index') }}">back to list</a>

    <a href="{{ path('app_job_type_edit', {'id': job_type.id}) }}">edit</a>

    {{ include('job_type/_delete_form.html.twig') }}
{% endblock %}
